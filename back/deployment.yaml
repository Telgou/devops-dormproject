apiVersion: v1
kind: Service
metadata:
  name: dbmysql-servicee
spec:
  selector:
    app: dbmysqll
  ports:
  - protocol: TCP
    port: 3306
    targetPort: 3306
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dbmysql-deploymentt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dbmysqll
  template:
    metadata:
      labels:
        app: dbmysqll
    spec:
      containers:
        - name: dbmysqll
          image: mysql:5.6
          ports:
            - containerPort: 3306
          imagePullPolicy: Always
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "root"
          volumeMounts:
            - name: db-volume
              mountPath: /var/lib/mysql
      volumes:
        - name: db-volume
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: dormproject-backendd
spec:
  selector:
    app: dormproject-backendd
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: NodePort
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dormproject-backendd
spec:
  replicas: 1 # or the desired number of replicas
  selector:
    matchLabels:
      app: dormproject-backendd
  template:
    metadata:
      labels:
        app: dormproject-backendd
    spec:
      containers:
        - name: dormproject
          image: telgou/dormproject:1
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:mysql://db-mysql:3306/backend?createDatabaseIfNotExist=true&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC"
            - name: SPRING_DATASOURCE_USERNAME
              value: "root"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "root"
  restartPolicy: Always
---